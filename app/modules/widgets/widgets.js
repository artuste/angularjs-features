(function() {
    'use strict';

    angular
        .module('app.widgets')
        .controller('Widgets', Widgets);

    Widgets.$inject = ['common'];

    function Widgets(common) {
        /*jshint validthis: true */
        var vm = this;

    }
})();
